#pragma once

#include "FractalMode.h"

class Mode1 : public FractalMode{
        public:
                Mode1(int x_pos, int y_pos, int n);
                void drawMode1(int x, int y, int n);
                

        private:


};